source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in hotcorg.gemspec
gemspec

group :development do
    gem "guard"
    gem "guard-rspec", "~> 4.7.0"
end