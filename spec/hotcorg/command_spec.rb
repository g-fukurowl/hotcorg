require "spec_helper"

module Hotcorg
    describe Command do
        #it "can get cpu temp (> 0 degree)" do
        #    expect(Command.cpu_temp).to be > 0
        #end

        Command.execute
    end
end
